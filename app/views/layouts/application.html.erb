<!DOCTYPE html>
<html>
  <head>
    <title>就活便利くん</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div class="container">
    <header>
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
          <div class="container-fluid">
          <a class="navbar-brand" href="#">就活便利くん</a>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul  class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item"><%= link_to "Home", root_path, {class: "nav-link"} %></li>
                <li class="nav-item"><%= link_to "ユーザー", p_users_edit_path, {class: "nav-link"} %></li>
                <li class="nav-item"><%= link_to "新規登録", p_users_new_path, {class: "nav-link",method: :get} %></li>
                <% if logged_in? %>
                  <li class="nav-item"><%= link_to "ログアウト", logout_path, {class: "nav-link",method: :delete} %></li>
                <% else %>
                  <li class="nav-item"><%= link_to "ログイン", login_path, {class: "nav-link",method: :get} %></li>
                <% end %>
              </ul>
            </div>
          </div>
        </nav>
    </header>

      <main>
        <div class="container">
          <a href="javascript:history.back()">
            <i class="bi bi-chevron-left"></i>
            </a>
          <%= yield %>
        </div>
      </main>

    </div>
  </body>
</html>
